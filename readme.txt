C++ code
Compile with any gcc, for example in unix(Mac)
	$ g++ Linker.cpp
Now copy paste the inputs into a text file and read it into the executable file on the terminal while executing it, like this in unix(Mac)
	$ ./a.out<input.txt
where,input.txt file has the inputs.